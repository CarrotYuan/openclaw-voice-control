# LaunchAgents

Public launchd templates should live here after sanitization.

Do not copy local plist files directly until:

- paths are template-based
- labels are finalized
- install instructions are stable
